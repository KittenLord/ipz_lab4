package work4;

/**
 * A drawable interface, which is kinda redundant tbh, but oh well
 */
public interface Drawable {

    /**
     * A function that draws very cool
     * @param x The X coordinate where to draw
     * @param y Where to draw on the Y axis
     */
    void draw(int x, int y);

}
